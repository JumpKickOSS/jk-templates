language = "kotlin"
framework = "spring-boot"
name = "webapp"
description = "Kotlin Spring Boot API + Vite/React SPA: a resource-only web module the app serves from classpath:static"
layouts = ["traditional"]
