spring.application.name=$name$
spring.threads.virtual.enabled=true
# Static resources come from classpath:/static/ — the web module's jar — by default; `/` is its
# index.html (Boot's welcome page), and SpaFallback serves it again for client-side routes.
